name: PowerShell code scan

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    uses: PolicyVault/.github/.github/workflows/powershell-code-scan.yml@ccc05a9266eab041a04fc8113f50179c3b65c837
